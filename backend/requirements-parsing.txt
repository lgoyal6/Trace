# Layout-aware document parsing (C31). Opt-in: NOT pulled into
# backend/requirements.txt, because the API request path never parses a PDF and
# these wheels are large. Install explicitly to run
# backend/measurement/run_parser_eval.py or backend/tests/test_layout_parse.py:
#     pip install -r backend/requirements-parsing.txt
pymupdf>=1.24
pdfplumber>=0.11

# C31 names LlamaIndex, and test_layout_parse.py's provenance round trip runs
# the parsed chunks through that framework's own node and retriever types
# rather than only the in-repo retriever. Same file, same opt-in, so they live
# here: without them that one test skips and the suite is a test short of its
# real count. Nothing on the API request path imports either package.
llama-index-core>=0.11
llama-index-retrievers-bm25>=0.3

# Exact hosted layout parser used by parse_layout_llamaparse. It remains
# opt-in because API use needs LLAMA_CLOUD_API_KEY and can incur cost.
llama-parse>=0.6,<0.7
